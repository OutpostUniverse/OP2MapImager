version: 2
jobs:
  build:
    docker:
      - image: outpostuniverse/circleci-gcc-googletest-freeimage
    steps:
      - checkout
      - run: git submodule update --init
      - run: make -k
      - run: make check
